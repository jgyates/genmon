[genneevo]
# Nee-Vo login credentials
username = 
password = 

# Display name that will show up in Genmon
tank_name = Propane Tank

# Tank capacity in gallons (used for display)
capacity = 

# How often to poll the API (in minutes)
poll_frequency = 60

# Optional: only use a specific tank by serial number (leave blank for first tank)
serial_number = 

# Debug logging
debug = False
